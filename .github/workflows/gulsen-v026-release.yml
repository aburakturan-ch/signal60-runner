name: Archived Gulsen Word Tools v0.2.6 build

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "v0.2.6 workflow archived. Use gulsen-v027-release.yml."
