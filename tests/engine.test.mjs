import assert from'node:assert/strict';import{universe,broadRank,candleMetrics,depthMetrics,deepScore}from'../runner/market-engine.mjs';
const rows=Array.from({length:120},(_,i)=>{const o=100*(1.001**i),c=o*1.001;return[i,o,c*1.002,o*.998,c,100,i+1,10000+i*100,100+i,50,5200+i*50]});const m=candleMetrics(rows);assert(m.momentum24>0);assert(m.trend>0);const d=depthMetrics({bids:[['99.9','20']],asks:[['100.1','20']]},100);assert(Number.isFinite(d.imbalance));const ex={symbols:[{symbol:'AAAUSDT',baseAsset:'AAA',quoteAsset:'USDT',status:'TRADING',isSpotTradingAllowed:true},{symbol:'BBBUSDT',baseAsset:'BBB',quoteAsset:'USDT',status:'TRADING',isSpotTradingAllowed:true}]},t=[{symbol:'AAAUSDT',lastPrice:'10',priceChangePercent:'5',quoteVolume:'1000000',count:5000},{symbol:'BBBUSDT',lastPrice:'2',priceChangePercent:'1',quoteVolume:'1000',count:20}],b=[{symbol:'AAAUSDT',bidPrice:'9.99',askPrice:'10.01'},{symbol:'BBBUSDT',bidPrice:'1.9',askPrice:'2.1'}];const r=broadRank(universe(ex,t,b));assert.equal(r[0].symbol,'AAAUSDT');const tf=Object.fromEntries(['5m','15m','1h','4h'].map(k=>[k,m]));assert(deepScore(r[0],tf,{imbalance:.4}).confidence>.5);console.log('PASS');
